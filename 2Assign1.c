#include<stdio.h>
#include<conio.h>
int main()
{
    int n;
    printf("Entre the number:");
    scanf("%d",&n);
    printf("unit digit of given number is %d",n%10);
    getch();
    return 0;
}