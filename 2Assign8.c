#include<stdio.h>
#include<conio.h>
int main()
{
    int a;
    float b;
    char c;
    double d;
    printf("size of int %d\nsize of float %d\nsize of char %d\nsize of double %d",sizeof(a),sizeof(b),sizeof(c),sizeof(d));
    getch();
    return 0;
}